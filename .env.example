# Shoplazza CD_Insure app – copy to .env.local and fill in real values.
# Never commit .env.local or put your Client Secret in code.

# From Partner Center → Apps → CD_Insure → App settings
SHOPLAZZA_CLIENT_ID=your_client_id
SHOPLAZZA_CLIENT_SECRET=your_client_secret

# Your app's base URL (no trailing slash). Use ngrok or similar for local dev.
# e.g. https://abc123.ngrok.io  or  https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Prisma (SQLite for local dev)
DATABASE_URL="file:./dev.db"

# Optional: for Shoplazza CLI (npm run dev:extension). Never commit real values.
# SHOPLAZZA_DEV_TOKEN=your_private_app_token
# SHOPLAZZA_DEV_STORE=https://your-store.myshoplaza.com/

# Optional: for production rate limiting (replace in-memory limiter in middleware).
# UPSTASH_REDIS_REST_URL=...
# UPSTASH_REDIS_REST_TOKEN=...
