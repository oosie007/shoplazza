export default function PolicyPage() {
  return (
    <div className="mx-auto max-w-4xl">
      <h1 className="text-2xl font-semibold text-zinc-900">Policy</h1>
      <p className="mt-2 text-zinc-600">
        List of all policies (insured orders) will appear here. Search by order
        ID, policy ID, or claim number; filter by claim type and status.
      </p>
    </div>
  );
}
